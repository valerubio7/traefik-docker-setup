# ============================================
# TRAEFIK GLOBAL CONFIGURATION
# ============================================
# Copia este archivo a .env y ajusta los valores

# Ambiente 
ENVIRONMENT=dev  # (dev o prod)

# Dominio principal para desarrollo
DEV_DOMAIN=localhost  # Usa .localhost para que funcione sin configurar /etc/hosts

# Dominio principal para producción
PROD_DOMAIN=example.com  # Ejemplo: tudominio.com

# Orígenes CORS para producción (opcional, separados por comas)
# Se agregará automáticamente https://PROD_DOMAIN y https://www.PROD_DOMAIN
# Formato: https://origin1,https://origin2
# Ejemplo: PROD_CORS_ORIGINS=https://app.example.com,https://api.example.com
PROD_CORS_ORIGINS=

# Email para Let's Encrypt (solo producción)
LETSENCRYPT_EMAIL=tu-email@example.com  # Let's Encrypt enviará notificaciones de renovación aquí

# Dashboard de Traefik
# Usuario y contraseña para acceder al dashboard
# IMPORTANTE: Genera un password hash seguro en producción
# Comando: htpasswd -nbB admin tu_password_seguro
# Formato esperado: admin:$2y$05$... (bcrypt) o admin:$apr1$... (apache MD5)
TRAEFIK_DASHBOARD_USER=admin
TRAEFIK_DASHBOARD_PASSWORD=admin

# IP Whitelist para Dashboard (solo producción)
# Lista de IPs permitidas para acceder al dashboard, separadas por comas
# Por defecto solo localhost. Para acceso remoto, agregar IPs específicas
# Formato: IP/CIDR,IP/CIDR
# Ejemplos:
#   - Solo localhost: 127.0.0.1/32
#   - Oficina + VPN: 127.0.0.1/32,203.0.113.0/24,10.0.1.0/24
#   - Red interna específica: 127.0.0.1/32,192.168.1.0/24
# NUNCA usar rangos amplios como 10.0.0.0/8 (16 millones de IPs)
DASHBOARD_ALLOWED_IPS=127.0.0.1/32

# Puertos
HTTP_PORT=80  # HTTP
HTTPS_PORT=443  # HTTPS
DASHBOARD_PORT=8080  # Dashboard (solo accesible desde localhost o con autenticación)

# Redes Docker
TRAEFIK_NETWORK=traefik-public  # Nombre de la red que compartirán todos tus proyectos

# Logging
LOG_LEVEL=INFO  # Nivel de log: DEBUG, INFO, WARN, ERROR

# Timezone
TZ=America/Argentina/Mendoza